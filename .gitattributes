# Normalize line endings to LF in the repo, checkout as-is on Windows
* text=auto eol=lf

# Shell scripts must keep LF
*.sh text eol=lf
probe/docker-entrypoint.sh text eol=lf

# Windows-only scripts can use CRLF
*.ps1 text eol=crlf
